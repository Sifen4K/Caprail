## 1. 项目骨架 + 应用外壳

- [x] 1.1 初始化 Tauri v2 项目（Rust 后端 + TypeScript 前端）
- [x] 1.2 配置项目结构：src-tauri（Rust）、src（前端）、assets
- [x] 1.3 实现系统托盘图标和右键菜单（截图、录屏、设置、退出）
- [x] 1.4 实现全局快捷键注册/注销（截图 Ctrl+Shift+A、录屏 Ctrl+Shift+R）
- [x] 1.5 实现设置持久化（JSON 配置文件读写，含快捷键、保存路径、默认格式）
- [x] 1.6 实现窗口关闭时最小化到托盘而非退出
- [x] 1.7 实现开机自启功能（Windows 启动项注册）

## 2. 截屏引擎（Rust 侧）

- [x] 2.1 集成 windows-rs crate，实现 DXGI Desktop Duplication 截屏
- [x] 2.2 实现全屏截取（指定显示器）
- [x] 2.3 实现区域裁剪（传入坐标矩形，返回裁剪后像素数据）
- [x] 2.4 实现窗口枚举（EnumWindows），获取所有可见窗口的位置和标题
- [x] 2.5 处理多显示器场景和不同 DPI 缩放的坐标变换
- [x] 2.6 实现 GDI BitBlt fallback（DXGI 不可用时的降级方案）

## 3. 截图选区覆盖层（前端）

- [x] 3.1 创建全屏无边框透明窗口（覆盖所有显示器）
- [x] 3.2 实现十字准线跟随鼠标
- [x] 3.3 实现拖拽选区（显示选区矩形边框 + 尺寸标注）
- [x] 3.4 实现窗口悬停高亮（Rust 侧传入窗口矩形列表，前端高亮匹配的窗口）
- [x] 3.5 实现选区确认（松开鼠标）和取消（Esc 键）
- [x] 3.6 实现全屏截图模式（双击选取当前显示器）

## 4. 标注编辑器（前端）

- [x] 4.1 创建标注编辑器窗口，加载截图到 Canvas
- [x] 4.2 实现工具栏 UI（工具按钮、颜色选择器、线宽滑块）
- [x] 4.3 实现矩形标注工具（拖拽绘制、自定义颜色/线宽）
- [x] 4.4 实现椭圆标注工具
- [x] 4.5 实现箭头标注工具
- [x] 4.6 实现自由线条（画笔）工具
- [x] 4.7 实现文字标注工具（点击输入文字，自定义字号/颜色）
- [x] 4.8 实现马赛克工具（区域像素化）
- [x] 4.9 实现模糊工具（区域高斯模糊）
- [x] 4.10 实现记号/印章工具（序号 ①②③、对勾、叉号、星号）
- [x] 4.11 实现撤销/重做（Ctrl+Z / Ctrl+Y）
- [x] 4.12 实现标注输出：复制到剪贴板（Ctrl+C）
- [x] 4.13 实现标注输出：保存为文件（Ctrl+S，支持 PNG/JPG）

## 5. 钉在屏幕上

- [x] 5.1 实现钉图浮窗创建（always-on-top 无边框窗口显示截图）
- [x] 5.2 实现浮窗鼠标拖动移动
- [x] 5.3 实现浮窗鼠标滚轮缩放（最小 50x50）
- [x] 5.4 实现浮窗透明度调节（Ctrl+滚轮，20%~100%）
- [x] 5.5 实现浮窗关闭（双击或 Esc）
- [x] 5.6 支持多实例浮窗共存

## 6. 录屏引擎（Rust 侧）

- [x] 6.1 实现 DXGI 帧捕获循环（60fps 目标帧率）
- [x] 6.2 实现 ffmpeg 子进程管理（启动、pipe 输入、停止）
- [x] 6.3 实现原始帧通过 stdin pipe 传入 ffmpeg 进行 H.264 编码
- [x] 6.4 实现录制暂停/恢复功能
- [x] 6.5 实现录制区域裁剪（仅捕获用户选定区域）
- [x] 6.6 实现录制状态 Tauri 事件（时长、帧率、状态推送到前端）

## 7. 录屏 UI（前端）

- [x] 7.1 复用截图选区覆盖层实现录屏区域选择
- [x] 7.2 实现录制状态指示器（悬浮工具栏：录制时长、暂停、停止按钮）
- [x] 7.3 实现录制控制交互（开始、暂停、恢复、停止）

## 8. 录屏剪辑 + 导出（前端 + Rust）

- [x] 8.1 创建剪辑预览窗口，加载录制视频
- [x] 8.2 实现时间轴控件（帧缩略图、起止手柄拖拽、点击定位）
- [x] 8.3 实现时间轴裁剪首尾（拖动起点/终点手柄）
- [x] 8.4 实现播放倍率选择（0.5x / 1x / 1.5x / 2x / 4x）
- [x] 8.5 实现 MP4 导出（Rust 侧调用 ffmpeg，支持裁剪 + 变速参数）
- [x] 8.6 实现 GIF 导出（ffmpeg palettegen + paletteuse，支持帧率/最大宽度配置）
- [x] 8.7 实现导出进度条显示和取消功能

## 9. OCR 文字提取

- [x] 9.1 集成 ONNX Runtime，加载 PaddleOCR 模型（检测 + 识别 + 方向分类）
- [x] 9.2 实现图片预处理（缩放、归一化）和推理 pipeline
- [x] 9.3 实现 OCR 结果后处理（文本行排序、合并）
- [x] 9.4 在标注编辑器中添加"提取文字"按钮和结果展示面板
- [x] 9.5 实现识别文字一键复制到剪贴板

## 10. 设置界面 + 分发

- [x] 10.1 实现设置窗口 UI（快捷键配置、保存路径、默认格式、开机自启开关）
- [x] 10.2 实现快捷键录入控件（按下组合键捕获 + 冲突检测提示）
- [x] 10.3 配置 Tauri 打包（NSIS 安装包，含 ffmpeg 和 OCR 模型文件）
- [x] 10.4 配置 Tauri updater 插件实现自动更新
- [x] 10.5 测试安装/卸载流程，确认注册表和快捷方式清理正常
